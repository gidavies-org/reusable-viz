name: Orchestrator

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

jobs:
  build:
    name: 'Build'
    steps:
     - name: Build summary
      run: |
        echo 'Build complete'
